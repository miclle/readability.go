{
  "cnn": {
    "fields": ["content", "textContent"],
    "reason": "current upstream keeps the outer smartassetcontainer (only the 'Powered by SmartAsset.com' attribution paragraph) while stripping the nested iframe/script payload. This port's embed cleanup removes the whole subtree. Replicating upstream would require a site-specific 'attribution-bearing embed wrapper' heuristic that risks regressing other widget/embed fixtures (calculators, social embeds, chart containers); reviewed and left as drift."
  },
  "firefox-nightly-blog": {
    "fields": ["byline", "content", "textContent"],
    "reason": "current upstream selects a newsletter block while pinned fixtures keep the article body"
  },
  "hukumusume": {
    "fields": ["content", "textContent"],
    "reason": "current upstream returns a shorter legacy table extraction than the pinned fixture"
  },
  "lifehacker-post-comment-load": {
    "fields": ["textContent"],
    "reason": "textContent whitespace around block boundaries differs"
  },
  "lifehacker-working": {
    "fields": ["textContent"],
    "reason": "textContent whitespace around block boundaries differs"
  },
  "medicalnewstoday": {
    "fields": ["byline", "content", "textContent"],
    "reason": "current upstream selects a print-message block while pinned fixtures keep the article body"
  },
  "wikipedia": {
    "fields": ["content"],
    "reason": "Go parser serializes an implicit tbody that current upstream omits"
  }
}
